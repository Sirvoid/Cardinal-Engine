namespace CardinalEngine {

    internal enum Opcode {
        AddEntity,
        RemoveEntity,
        UpdateEntityPosition,
        SyncEntityField,
        AddEntityComponent,
        RemoveEntityComponent,
        SetNetworkId
    }
}